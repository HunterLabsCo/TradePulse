// BottomNav — disabled during Mist redesign.
// All app shell pages (Index, Journal, Settings) now have built-in Mist tab bars.
// To re-enable: restore the original BottomNav implementation.
export function BottomNav() {
  return null;
}
